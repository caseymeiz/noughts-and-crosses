define(['../constants'],function (constants) {
    function Render () {
    };

    Render.prototype = {
        constructor : Render,

        view : function (gamestate) {
            var game = document.createSVG
        }
    };

    return Render;
});